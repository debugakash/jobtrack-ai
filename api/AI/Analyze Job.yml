info:
  name: Analyze Job
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/api/ai/jobs/{{jobId}}/analyze"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
