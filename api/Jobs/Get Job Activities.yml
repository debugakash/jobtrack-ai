info:
  name: Get Job Activities
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/api/jobs/{{jobId}}/activities"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
