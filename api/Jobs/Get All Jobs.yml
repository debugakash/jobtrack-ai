info:
  name: Get All Jobs
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/jobs"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
