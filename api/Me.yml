info:
  name: Me
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/auth/me"
  headers:
    - name: Authorization
      value: Bearer abc123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
