info:
  name: Delete Account
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{BASE_URL}}/api/auth/me"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
