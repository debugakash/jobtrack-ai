info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "bruno.mars@gmail.com",
        "password": "BrunoPass1"
      }

      // {
      //   "email": "laniho7132@careney.com",
      //   "password": "JobTrack@123"
      // }

      // {
      //   "email": "baboy37353@beiwoh.com",
      //   "password": "MyResendPass@1234"
      // }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        if (res.status === 200) {
          const body = res.getBody();

          bru.setEnvVar("accessToken", body.data.accessToken);

          console.log("Access token saved.");
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
