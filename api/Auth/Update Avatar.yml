info:
  name: Update Avatar
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{BASE_URL}}/api/auth/me/avatar"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: multipart-form
    data:
      - name: avatar
        type: file
        value:
          - D:\Work\hacker-5471975_1920.png
        contentType: image/png
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
