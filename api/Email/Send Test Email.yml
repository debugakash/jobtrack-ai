info:
  name: Send Test Email
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/api/email-test"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
