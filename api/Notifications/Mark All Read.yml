info:
  name: Mark All Read
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/api/notifications/read-all"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
