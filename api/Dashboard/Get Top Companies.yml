info:
  name: Get Top Companies
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/dashboard/top-companies"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
