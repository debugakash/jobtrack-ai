info:
  name: Get Status Distribution
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/dashboard/status-distribution"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
