info:
  name: Get Dashboard
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/api/dashboard/upcoming-interviews"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
