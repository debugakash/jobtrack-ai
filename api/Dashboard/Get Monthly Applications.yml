info:
  name: Get Monthly Applications
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/dashboard/monthly-applications"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
