info:
  name: Get Pending Follow-ups
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/dashboard/follow-ups"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
