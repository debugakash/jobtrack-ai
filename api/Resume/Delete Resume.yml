info:
  name: Delete Resume
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BASE_URL}}/api/resumes/{{resumeId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
