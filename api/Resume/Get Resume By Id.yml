info:
  name: Get Resume By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/resumes/{{resumeId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
