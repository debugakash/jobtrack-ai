info:
  name: Delete Interview
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BASE_URL}}/api/interviews/{{interviewId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
